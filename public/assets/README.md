Drop these files here for the Nav + Hero section:

1. `hero-bg.png` - illustrated background used behind hero text.
2. `mascot-pip.png` - mascot character shown on the right side of hero.

You can use `.jpg`, `.webp`, or `.svg` too, but then update paths in `app/page.tsx`.
